// ### to round result of interpolateNumber

// same a, b
export default function(a, b) {

    // same t
  return a = +a, b -= a, function(t) {

      // round up result
    return Math.round(a + b * t);
  };
}
